var score;
score = 80;

// this is same as

var score = 80;

// var score; is executed before runtime
// score = 80; is executed at runtime

console.log(example);
var example = "defining value is executed before runtime";
console.log(example);

console.log(example2);
example2 = 80;
var example2;

console.log(example2);
