const a = 2100;
const b = "2100";
// ==  operator first make two variable into same type.
// than compare the value
console.log(`a == b ? ${a == b}`);
console.log(`true == 1 ${true == 1}`);
console.log(`a === b ? ${a === b}`);
// so lets not use == because is strange
