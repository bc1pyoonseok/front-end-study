console.log(score);

// initialed in 0x0001
var score;
// assigned in another address because it is object
score = 80;

console.log(score);

console.log(myName);

// this one is also same with above
var myName = "yoonseok";

console.log(myName);

console.log(hisName);

// this one is also same with above
hisName = "nakamoto";
var hisName;

console.log(hisName);
